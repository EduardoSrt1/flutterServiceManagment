name: atendimentos_app
description: Aplicativo Flutter de gestão de atendimentos (exemplo gerado)
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
  sqflite: ^2.2.8+4
  path_provider: ^2.0.14
  path: ^1.8.3
  image_picker: ^0.8.7+5
  get_it: ^7.6.0
  injectable: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  injectable_generator: ^2.2.0

flutter:
  uses-material-design: true
